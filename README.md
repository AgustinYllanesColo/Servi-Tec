# Vite React Project

Este proyecto usa Vite + React. Para ejecutar localmente:

```bash
npm install
npm start
```

Luego abrir `http://localhost:5173` (o el puerto que indique Vite).

Para generar una versión de producción:

```bash
npm run build
npm run preview
```
